# YouTube API Key for fetching video metadata
YOUTUBE_API_KEY=your_youtube_api_key_here

# Custom proxy URL (if you want to use your own proxy instead of VPNBook)
# YTDLP_PROXY_URL=http://username:password@proxy-server:port

# VPNBook proxy settings
USE_VPNBOOK=True
# VPNBOOK_COUNTRY=US  # Options: US, CA, DE, FR, UK, PL (leave blank for random)
# VPNBOOK_PROTOCOL=http  # Options: http, socks

# FFMPEG path (uncomment and set if ffmpeg is not in PATH)
# FFMPEG_PATH=/path/to/ffmpeg 